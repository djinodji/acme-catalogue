{% extends 'AppBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} - Creation Album
{% endblock %}

{% block top %}
    <section id="home" xmlns="http://www.w3.org/1999/html">


            <div class="custom-caption">
                <table class="table table-responsive">
                <h1>Produit creation</h1>
                    <div class="col-lg-4">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    </div>
                    <div class="col-lg-12">
                    <input class="btn btn-primary btn-sm" type="submit" value="Create"/>

                    <a class="btn btn-primary btn-sm" href="{{ path('produit_index') }}">Back to the list</a>
                    </div>
                    {{ form_end(form) }}


</table>
            </div>


    </section>


{% endblock top %}


            {% block content %}
                <div class="carousel-inner">
                    <div class="row">
                        <div class="col-sm-12 col-md-9">

                        </div>
                    </div>
                </div>

            {% endblock %}
