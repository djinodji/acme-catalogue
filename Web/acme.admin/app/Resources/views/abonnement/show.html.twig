{% extends 'AppBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} - Album
{% endblock %}

{% block top %}
<section id="home">

    <div class="item">

        <div class="custom-caption">
    <h1>Abonnement</h1>

    <table class="table table-responsive">
        <tbody>
            <tr>
                <th>Utilisateur</th>
                <td>{{ abonnement.utilisateur.username }}</td>
            </tr>
            <tr>
                <th>Album</th>
                <td>{{ abonnement.album.nom }}</td>
            </tr>
        <tr colspan="2">
            <th >
                <div class="col-lg-12">
                <a class="btn btn-primary btn-sm" href="{{ path('abonnement_index') }}">Retourner sur la liste</a>

                <a class="btn btn-primary btn-sm" href="{{ path('abonnement_edit', { 'id': abonnement.id }) }}">Moifier</a>
<div class="col-lg-2">
                {{ form_start(delete_form) }}
                <input class="btn btn-primary btn-sm" type="submit" value="Supprimer">
                {{ form_end(delete_form) }}
</div>
                </div>
            </th>
        </tr>
        </tbody>
    </table>



</div>
        </div>
</section>



{% endblock %}

